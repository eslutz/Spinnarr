# Spinnarr Configuration Example
# Copy this file to .env and update the values as needed

# Timezone for container logs (optional)
TZ=America/New_York

# Port mapping (host:container)
# Change the first number to expose Spinnarr on a different port
PORT=8080

# Optional: Path to custom spinner configurations
SPINNERS_PATH=./spinners
